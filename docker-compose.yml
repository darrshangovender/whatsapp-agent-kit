services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      WA_DB_PATH: /data/wa_kit.db
    volumes:
      - wa_state:/data
    restart: unless-stopped

volumes:
  wa_state:
